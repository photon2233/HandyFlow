{
  "name": "handyflow",
  "version": "0.1.0",
  "description": "HandyFlow — Chrome extension for touchless web control via hand gestures.",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch-model": "node scripts/fetch-model.js",
    "dev": "npm run fetch-model && vite",
    "build": "npm run fetch-model && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.25",
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5",
    "vite": "^5.2.10",
    "vite-plugin-static-copy": "^1.0.4"
  }
}
